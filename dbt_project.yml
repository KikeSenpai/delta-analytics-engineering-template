name: 'delta_analytics'
version: '1.0.0'
config-version: 2

profile: 'delta_analytics'

seeds:
  delta_analytics:
    +schema: raw          # Seeds land in main.raw — fixture source data
    +file_format: delta   # Delta format so source matches prod

models:
  delta_analytics:
    # Delta format for all models — Databricks-style managed tables
    +file_format: delta
    +materialized: table
    staging:
      +materialized: view
