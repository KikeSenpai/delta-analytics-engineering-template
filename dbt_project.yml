name: 'delta_analytics'
version: '1.0.0'
config-version: 2

profile: 'delta_analytics'

seeds:
  delta_analytics:
    +file_format: delta   # Delta format so seeds match prod

models:
  delta_analytics:
    # Delta format for all models — Databricks-style managed tables
    +file_format: delta
    +materialized: table
