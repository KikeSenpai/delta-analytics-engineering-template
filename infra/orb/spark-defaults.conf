# Delta Lake session extensions
spark.sql.extensions             io.delta.sql.DeltaSparkSessionExtension
spark.sql.catalog.spark_catalog  org.apache.spark.sql.delta.catalog.DeltaCatalog

# Unity Catalog — prod catalog (Databricks-style 3-level namespace)
# Orb-native: UC server runs on localhost:8090 (not Docker network name)
spark.sql.catalog.prod          io.unitycatalog.spark.UCSingleCatalog
spark.sql.catalog.prod.uri      http://localhost:8090
spark.sql.catalog.prod.token    ""
spark.sql.defaultCatalog         prod

# Ivy cache to /tmp (avoids permission issues)
spark.driver.extraJavaOptions    -Divy.cache.dir=/tmp -Divy.home=/tmp

# Memory limits for orb (constrained RAM — 3.8 GB total)
spark.driver.memory              1g
spark.driver.maxResultSize       256m
