# Delta Lake session extensions
spark.sql.extensions             io.delta.sql.DeltaSparkSessionExtension
spark.sql.catalog.spark_catalog  org.apache.spark.sql.delta.catalog.DeltaCatalog

# Unity Catalog — main catalog (Databricks-style 3-level namespace)
spark.sql.catalog.main           io.unitycatalog.spark.UCSingleCatalog
spark.sql.catalog.main.uri       http://unity-catalog:8080
spark.sql.catalog.main.token     ""
spark.sql.defaultCatalog         main

# MinIO S3A — optional, used when MinIO profile is enabled
spark.hadoop.fs.s3a.endpoint     http://minio:9000
spark.hadoop.fs.s3a.path.style.access  true
spark.hadoop.fs.s3a.connection.ssl.enabled  false
spark.hadoop.fs.s3a.impl         org.apache.hadoop.fs.s3a.S3AFileSystem

# Ivy cache to /tmp (avoids permission issues in container)
spark.driver.extraJavaOptions    -Divy.cache.dir=/tmp -Divy.home=/tmp
