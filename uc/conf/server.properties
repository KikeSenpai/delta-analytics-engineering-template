# Unity Catalog server configuration
# https://docs.unitycatalog.io/server/configuration/

server.env=dev
server.authorization=disable

# Enable experimental managed table support so dbt CREATE TABLE ... USING DELTA
# creates UC-managed tables with server-allocated storage (Databricks-style).
server.managed-table.enabled=true
